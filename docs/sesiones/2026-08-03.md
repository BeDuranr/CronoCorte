# 2026-08-03

## Análisis del agente Claude Code de Crono Corte
- Revisado `.mcp.json` (servidor Supabase en modo read-only), `.claude/settings.local.json` (permisos, y un `SUPABASE_ACCESS_TOKEN` en texto plano — pendiente de resolver), `.claude/commands/` (`/db-types`, `/new-migration`), y `cronocorte/CLAUDE.md` (memoria de proyecto, antes un solo archivo grande).

## Subagentes especializados creados (`.claude/agents/`)
- `db-migrations` — schema real, deuda técnica de columnas legacy, convención de migraciones, MCP de Supabase read-only.
- `whatsapp-flows` — rutas de WhatsApp, reglas duras (agente IA desacoplado, umbrales de verificación de pago, sanitización de variables de template).
- `reviewer` — checklist de revisión (seguridad → reglas de negocio → timezone → agente IA → calidad), sin permiso de `Edit` (solo lectura).

## Hooks configurados (`.claude/settings.json` + `.claude/hooks/`)
- `typecheck-on-edit.mjs` (PostToolUse en `.ts`/`.tsx`): corre `tsc --noEmit --incremental` en vez de `next lint`, porque el proyecto no tiene `.eslintrc` y `next lint` colgaría el hook con su prompt interactivo.
- `block-push-if-tests-fail.mjs` (PreToolUse en `git push`): corre `vitest run` y bloquea el push si algo falla.
- `remind-db-types.mjs` (PostToolUse en `supabase/*.sql`): recuerda correr la migración en el Dashboard y `/db-types` si cambiaron columnas.

## Memoria reestructurada de un archivo grande a modular
- `cronocorte/CLAUDE.md` dividido en `docs/arquitectura.md`, `docs/flujo-reservas.md`, `docs/flujo-pagos-whatsapp.md`, `docs/agente-ia.md`, `docs/barberos-acceso.md`.
- `ANALISIS_MEJORAS.md` (raíz del proyecto) migrado a `docs/pendientes.md` (backlog vivo) + `docs/sesiones/2026-06-26.md` (su changelog fechado, que en realidad era una entrada de sesión mezclada con el backlog).
- Nueva carpeta `docs/sesiones/` con un índice (`00-indice.md`) y un archivo por fecha, para no tener que leer todo el historial.

## Pendiente para la próxima sesión
- Decidir qué hacer con el `SUPABASE_ACCESS_TOKEN` en texto plano de `.claude/settings.local.json`.
- Evaluar si se quiere un command `/log-session` para automatizar el cierre de sesión (propuesto, no creado aún).
