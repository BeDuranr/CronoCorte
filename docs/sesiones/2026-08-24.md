# 2026-08-24

## Revisión de `docs/pendientes.md` vs. código real
- Varios ítems del backlog estaban desactualizados:
  - El bug "webhook aplica comprobante a la cita más próxima" ya estaba resuelto desde jul-2026 (`src/lib/receipt-matching.ts`: `selectReceiptTarget` + `matchRecipient`, con tests) pero seguía listado como pendiente.
  - El cambio de display name de WhatsApp ("JamonBarber" → "CronoCorte", con número nuevo) ya se completó (certificado SII + verificación Meta), pero el doc lo listaba como bloqueado.
  - Las 4 optimizaciones y el bug de `schema.sql` desincronizado sí seguían pendientes de verdad (confirmado leyendo el código).
- Moralaje: el backlog no se estaba actualizando al resolver items — revisarlo contra el código de vez en cuando antes de confiar en él ciegamente.

## `schema.sql` regenerado
- Estaba congelado desde 2026-07-13; faltaban 4 migraciones ya aplicadas en producción: `20260722_blocked_slots_policies`, `20260724_add_slot_interval_minutes`, `20260818_create_barbershop_logos_bucket`, `20260818_fix_barbershop_logos_policies`.
- Regenerado con el subagente `db-migrations`. Verificación tabla/columna por columna contra el OpenAPI que expone PostgREST (`GET /rest/v1/` con la service-role key) — no hubo drift adicional al ya conocido por los 4 archivos sueltos.
- **Limitación conocida**: el MCP de Supabase (`mcp__supabase__list_tables`/`execute_sql`) no estuvo disponible en esa sesión del subagente, así que las policies RLS, índices y constraints de exclusión se transcribieron desde los 4 archivos de migración (no se re-verificaron en vivo contra `pg_policies`/`pg_indexes`). Los archivos de migración son la fuente de verdad documentada, así que el riesgo es bajo, pero queda anotado por si alguien quiere cerrar el loop con acceso MCP real.
- Se descartó migrar a Supabase CLI (`supabase init` + `db push`) por ahora — se sigue con el flujo de archivos `.sql` sueltos aplicados a mano en el Dashboard. `schema.sql` es solo la foto de documentación, no cambia el flujo de trabajo.

## Pendiente para la próxima sesión
- Backlog de bugs quedó en cero. Quedan las 4 optimizaciones de `docs/pendientes.md` (middleware consultando `user_profiles` por request, uso de más del service role, N+1 queries de conflictos en `create`, `booking-flow.tsx` de 1252 líneas) para cuando haya tiempo.
- Si en algún momento se necesita confiar 100% en las policies/índices de `schema.sql`, conviene re-verificarlas con acceso MCP real a Supabase en vez de transcripción manual.
