# 2026-06-26

## Limpieza del proyecto
- Eliminados archivos innecesarios de la raíz: `estilos_cronocorte.html`, `estilos_modos.html`, `logo-opciones.html`, `logos_cronocorte.html`, `CronoCorte_Planificacion.docx` (v1)
- Eliminados 5 archivos `*.old` del caché de webpack en `.next/cache/`
- Agregado `supabase/.temp/` a `.gitignore` (contenía ref del proyecto Supabase — repo es público)

## Tipos TypeScript refactorizados
- `src/types/database.ts` ahora deriva tipos del schema real de Supabase en lugar de interfaces escritas a mano
- Agregado `src/types/supabase.ts` (auto-generado con el command `/db-types`)

## Modal de aviso WhatsApp
- Nuevo componente `WhatsAppPendingModal` en `booking-flow.tsx`
- Se muestra al completar la reserva, explica al cliente que recibirá un mensaje de WhatsApp en segundos
- Botón "Entendido" bloqueado 5 segundos para asegurar que lo lean
- Soluciona el problema de clientes que usaban el botón manual antes de recibir el mensaje automático

## Migración npm → pnpm
- Reemplazado `package-lock.json` por `pnpm-lock.yaml`
- Creado `pnpm-workspace.yaml` con aprobación de build scripts para `unrs-resolver`
- Comandos: `pnpm dev`, `pnpm build`, `pnpm install`

## Dominio propio `cronocorte.com`
- Comprado en Vercel (~US$11.25/año)
- Vinculado al proyecto con SSL automático
- Actualizado en todas las integraciones:
  - `NEXT_PUBLIC_APP_URL` en Vercel → `https://cronocorte.com`
  - Webhook de Twilio → `https://cronocorte.com/api/whatsapp/webhook`
  - Supabase Auth Site URL → `https://cronocorte.com`
  - Supabase Redirect URLs → `https://cronocorte.com/**`

## Cambio de display name WhatsApp (quedó pendiente)
- Ticket abierto en Twilio para cambiar "JamonBarber" → "CronoCorte"
- Bloqueado por verificación de negocio en Meta (requiere certificado SII)
- Logo SVG generado (`cronocorte-logo.svg`, 640×640px) y subido a Twilio
- Retomar cuando se tenga inicio de actividades en SII
